pre-commit:
  parallel: true
  commands:
    secrets:
      run: gitleaks git --staged --redact --no-banner
    shell:
      run: bash scripts/hook-shell.sh
    actionlint:
      glob: ".github/workflows/*"
      run: actionlint
    policy:
      run: bash scripts/policy.sh
