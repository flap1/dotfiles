#!/bin/bash

mapfile -t dangling_images < <(docker images -f "dangling=true" -q)

if [ ${#dangling_images[@]} -gt 0 ]; then
    docker rmi "${dangling_images[@]}"
else
    echo "No dangling images found. Skipping removal."
fi
