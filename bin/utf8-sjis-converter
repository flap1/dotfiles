#!/bin/bash

file=$1

codec=`nkf -g $file` # codec取得
echo $codec $file

if [[ $codec == "Shift_JIS" ]] ; then
    iconv -c -f SHIFT_JIS -t UTF-8 $file > "utf8_${file}"
elif [[ $codec == "UTF-8" ]] ; then
    iconv -c -f UTF-8 -t SHIFT_JIS $file > "sjis_${file}"
fi
