# paste-shot

Optional. A Windows screenshot reaches a Claude Code session on the Linux
box over an ssh LocalForward. Not part of `install.sh`.

Linux (receiver):

```bash
.config/paste-shot/setup.sh
```

Windows (hotkey):

```powershell
.config\paste-shot\setup.ps1 -Start
```
