let g:todoist = {
\ 'icons': {
\   'unchecked': '  ',
\   'checked':   '  ',
\   'loading':   '  ',
\   'error':     '  ',
\ },
\}

" function! s:todoist_install ()
"   const cmd = printf('cd %s && npm install', s:plugin_root)
"   echom "Todoist: Installing dependencies..."
"   echom cmd
"   call system(cmd)
"   echom "Todoist: Updating remote plugins..."
"   UpdateRemotePlugins
"   echom "Todoist: Done"
" endfunc

" call s:todoist_install()

" TodoistInstall
" UpdateRemotePlugins

