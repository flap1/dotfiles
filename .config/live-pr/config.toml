[diff]
# Keep the conversation compact and give the embedded CodeDiff review room.
command = 'nvim -c "CodeDiff --inline $LIVE_PR_RANGE"'
commit_command = 'nvim -c "CodeDiff $LIVE_PR_SHA~1 $LIVE_PR_SHA"'
split_ratio = 20
min_pane_width = 60
